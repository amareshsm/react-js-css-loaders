export const MoonLoader = `const React = require('react')
const ReactDOM = require('react-dom/server')
const { Moon } = require('react-js-css-loaders')

const el = React.createElement(Moon)
const html = ReactDOM.renderToString(el)
`

export const RevolverLoader = `const React = require('react')
const ReactDOM = require('react-dom/server')
const { Revolver } = require('react-js-css-loaders')

const el = React.createElement(Revolver)
const html = ReactDOM.renderToString(el)
`

export const ChaserLoader = `const React = require('react')
const ReactDOM = require('react-dom/server')
const { Chaser } = require('react-js-css-loaders')

const el = React.createElement(Chaser)
const html = ReactDOM.renderToString(el)
`

export const RhythmLoader = `const React = require('react')
const ReactDOM = require('react-dom/server')
const { Rhythm } = require('react-js-css-loaders')

const el = React.createElement(Rhythm)
const html = ReactDOM.renderToString(el)
`

export const ElipsisLoader = `const React = require('react')
const ReactDOM = require('react-dom/server')
const { Elipsis } = require('react-js-css-loaders')

const el = React.createElement(Elipsis)
const html = ReactDOM.renderToString(el)
`

export const ThrobberLoader = `const React = require('react')
const ReactDOM = require('react-dom/server')
const { Throbber } = require('react-js-css-loaders')

const el = React.createElement(Throbber)
const html = ReactDOM.renderToString(el)
`

export const RevolutionLoader = `const React = require('react')
const ReactDOM = require('react-dom/server')
const { Revolution } = require('react-js-css-loaders')

const el = React.createElement(Revolution)
const html = ReactDOM.renderToString(el)
`

export const SquareLoader = `const React = require('react')
const ReactDOM = require('react-dom/server')
const { Square } = require('react-js-css-loaders')

const el = React.createElement(Square)
const html = ReactDOM.renderToString(el)
`

export const CardflipingLoader = `const React = require('react')
const ReactDOM = require('react-dom/server')
const { Cardfliping } = require('react-js-css-loaders')

const el = React.createElement(Cardfliping)
const html = ReactDOM.renderToString(el)
`

export const DewdropsLoader = `const React = require('react')
const ReactDOM = require('react-dom/server')
const { Dewdrops } = require('react-js-css-loaders')

const el = React.createElement(Dewdrops)
const html = ReactDOM.renderToString(el)
`

export const SpinnerLoader = `const React = require('react')
const ReactDOM = require('react-dom/server')
const { Spinner } = require('react-js-css-loaders')

const el = React.createElement(Spinner)
const html = ReactDOM.renderToString(el)
`

export const WhirlLoader = `const React = require('react')
const ReactDOM = require('react-dom/server')
const { Whirl } = require('react-js-css-loaders')

const el = React.createElement(Whirl)
const html = ReactDOM.renderToString(el)
`

export const EaseinoutLoader = `const React = require('react')
const ReactDOM = require('react-dom/server')
const { Easeinout } = require('react-js-css-loaders')

const el = React.createElement(Easeinout)
const html = ReactDOM.renderToString(el)
`
